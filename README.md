# Predicting Bikeshare Patterns

## Prerequisites

You need to have Python, numpy and anaconda install on your computer

## Install Dependency and run the notebook

Use `pip install -r requirements.txt` command
